<script type="text/javascript">
    $(document).ready(function () {
        var fields = $('tr[page="1"]');
        for (var i = 0; i < fields.length; i++) {
            fields[i].style.display = '';
            fields[i].setAttribute('data-visible', 'true');
            fields[i].setAttribute('id', 'active_page');
        }
            $('label[for="1"]')[0].setAttribute('id', 'active_page')
    }
    );

    function pager(page) {
        var fields = $('tr[page="' + page + '"]');
        var fields_visible = $('tr[data-visible="true"]');
       var button_click = document.getElementById('active_page').getAttribute('for');
       console.log(button_click, document.getElementById('active_page').getAttribute('for'));
        $('label[for="' + Namber.parseInt(button_click) + '"]')[0].removeAttribute('id');
        for (var i = 0; i < fields_visible.length; i++) {
            fields_visible[i].style.display = 'none';
            fields_visible[i].setAttribute('data-visible', 'false');
        }
        for (var i = 0; i < fields.length; i++) {
            fields[i].style.display = '';
            fields[i].setAttribute('data-visible', 'true');
            $('label[for="'+ page +'"]')[0].setAttribute('id', 'active_page');
        }
    }
</script>